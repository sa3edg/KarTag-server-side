package com.kartag.business.util;

import com.kartag.server.model.User;

public class UserUtils {

	public static User mergeUser(User sent, User cashed){
		User merged = new User();
		return merged;
	}
}
